apiVersion: v1
kind: Service
metadata:
  name: ndbskjhsyie
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "ndbskjhsyie"
    port: 8080
  selector:
    app: ndbskjhsyie
